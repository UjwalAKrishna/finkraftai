{
  "roles": [
    {
      "name": "Admin",
      "permissions": ["filter_data", "export_report", "create_ticket"],
      "can_see_traces": true,
      "description": "Full access to all tools and traces"
    },
    {
      "name": "Manager", 
      "permissions": ["filter_data", "export_report", "create_ticket"],
      "can_see_traces": false,
      "description": "Can filter, export and create tickets"
    },
    {
      "name": "Viewer",
      "permissions": ["filter_data"],
      "can_see_traces": false, 
      "description": "Can only view and filter data"
    }
  ]
}